export enum FunctionalityEnum {

    // Administration Funcs
    ADMINISTRATION = 1000,
    USERS = 2,
    GROUPS = 3,
    AUTHENTICATION = 4,
    CLEAN_LOG = 16,
    HOLIDAY = 17,

    // Processes Funcs
    PROCESSES = 2000,
    PARAMETER = 14,
    AGENT = 7,
    PROCESS = 5,
    EXECUTOR = 6,
}
